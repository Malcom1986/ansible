- hosts: webservers
  gather_facts: no

  tasks:

    - name: update apt cache
      ansible.builtin.apt:
        update_cache: yes
      become: yes
      tags: cache

    - name: install git
      ansible.builtin.apt:
        name: git
        state: present
      become: yes
      tags: install

